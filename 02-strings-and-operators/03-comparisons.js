const userAge = 17;
const minAge = 18;
const userInput = '18';

console.log(`userAge >= minAge: ${userAge >= minAge}`);
console.log(`userInput === minAge: ${userInput === minAge}`);
console.log(`userInput == minAge: ${userInput == minAge}`);
